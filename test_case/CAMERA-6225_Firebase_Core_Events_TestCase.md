# CAMERA-6225 Android Firebase Core Events (UXI & Camera Related Events) 測試案例

## 📋 測試概述

**Ticket**: CAMERA-6225  
**功能**: [Android] [Stage 1] Implementation for Firebase Core Events (UXI & Camera related Events)  
**Parent Epic**: [CAMERA-6050](https://alfredlabs.atlassian.net/browse/CAMERA-6050) - Firebase Analytics Reboot – Stage 1 (Core User Properties + UXI + Camera Related Events)  
**技術規格文件**: 
- [Event Name and Property Validation](https://alfredlabs.atlassian.net/wiki/spaces/PD/pages/3900801027/Event+Name+and+Property+Validation)
- [Login UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI)
- [Live UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI)
- [CR Playback UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI)
- [Event Play UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI)
- [Payment UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI)
- [Camera Active v2](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2)
**Fix Versions**: Android - 2026.1.0  
**建立日期**: 2025-12-22  
**負責人**: QA Team - Jessie  
**狀態**: Testing

**產品專有名詞參考**：本測試案例中使用的專有名詞請參考 [Terminology Table v2](https://alfredlabs.atlassian.net/wiki/spaces/~590960658/pages/3834085744/Terminology+Table+v2)

### 變更重點
本 PR 為 Firebase Analytics Reboot Stage 1 的實作，主要包含：
1. 實作 Firebase 事件名稱和屬性驗證機制（僅在 Development Build 中）
2. 上報多個新的 Firebase Analytics 事件（UXI 和 Camera 相關事件）
3. Register Device API 新增 `instance_id` 欄位（Firebase instance id）

**重要原則**：
- ⚠️ **不改動任何舊的事件！**
- 新事件必須使用正確的 data type
- 事件驗證僅在 Development Build 中執行，Production Build 不執行驗證

### 核心變更
1. **Firebase 事件驗證機制**：
   - Event Name Validation：僅允許小寫字母、數字、底線，必須以小寫字母開頭，不能有連續底線
   - Event Property Validation：與 Event Name 相同的驗證規則
   - 僅在 Development Build 中執行驗證
   - 驗證失敗時顯示 Toast 訊息並取消事件上報

2. **新增 Firebase Analytics 事件（8 個）**：
   - `login_experience_v2`：與舊版 `login_experience` 內容相同
   - `live_experience_v2`：與舊版 `live_experience` 內容相同
   - `cr_playback_experience_v2`：與舊版 `continuous_recording_playback_experience` 內容相同
   - `event_play_experience_v2`：之前沒有上報到 Firebase（新增）
   - `payment_experience_v2`：之前沒有上報到 Firebase（新增），需包含 `url_query` 欄位（超過 100 字元需剪裁）
   - `camera_active_v2`：Camera 相關事件（新增）
   - `camera_statistics_v2`：Camera 相關事件（新增）
   - `camera_settings_v2`：Camera 相關事件（新增）

3. **Register Device API 變更**：
   - 新增 `instance_id` 欄位，內容為 Firebase 的 instance id (app_instance_id)
   - 參考：[CAMERA-5617](https://alfredlabs.atlassian.net/browse/CAMERA-5617)

---

## 🎯 測試重點與風險評估

### 影響模組/功能
1. **Firebase Analytics 事件上報機制** - 高影響
   - 新增事件驗證機制（僅 Development Build）
   - 新增多個 UXI 和 Camera 相關事件
   - 影響所有觸發這些事件的用戶操作流程

2. **登入流程 (Login Flow)** - 中影響
   - `login_experience_v2` 事件上報
   - 需驗證與舊版 `login_experience` 內容相同

3. **即時影像流程 (Live Feed)** - 中影響
   - `live_experience_v2` 事件上報
   - 需驗證與舊版 `live_experience` 內容相同

4. **連續錄影播放流程 (CR Playback)** - 中影響
   - `cr_playback_experience_v2` 事件上報
   - 需驗證與舊版 `continuous_recording_playback_experience` 內容相同

5. **事件播放流程 (Event Playback)** - 中影響
   - `event_play_experience_v2` 事件上報（新增）
   - 需驗證事件正確上報到 Firebase

6. **訂閱購買流程 (Payment)** - 中影響
   - `payment_experience_v2` 事件上報（新增）
   - 需驗證 `url_query` 欄位正確處理（超過 100 字元需剪裁）

7. **相機相關功能 (Camera Related)** - 中影響
   - `camera_active_v2`、`camera_statistics_v2`、`camera_settings_v2` 事件上報
   - 影響相機配對、設定、統計等功能

8. **Register Device API** - 中影響
   - 新增 `instance_id` 欄位
   - 影響裝置註冊流程

### 迴歸風險區
1. **現有 Firebase Analytics 事件**：
   - 所有舊的事件不應被改動
   - 需驗證舊事件仍正常上報

2. **登入流程**：
   - 登入功能正常運作
   - 登入體驗不受影響

3. **即時影像功能**：
   - 即時串流功能正常
   - 串流體驗不受影響

4. **連續錄影播放功能**：
   - CR 播放功能正常
   - 播放體驗不受影響

5. **事件播放功能**：
   - 事件播放功能正常
   - 播放體驗不受影響

6. **訂閱購買功能**：
   - 訂閱購買流程正常
   - 購買體驗不受影響

7. **相機配對與設定功能**：
   - 相機配對功能正常
   - 相機設定功能正常
   - 相機統計功能正常

8. **裝置註冊功能**：
   - 裝置註冊流程正常
   - 向下兼容性驗證

### 具體測試場景建議（含邊緣案例）
1. **事件驗證機制測試**：
   - Development Build 中驗證失敗時顯示 Toast 訊息
   - Production Build 中不執行驗證
   - 驗證失敗的事件不會上報到 Firebase

2. **新事件上報測試**：
   - 驗證新事件正確上報到 Firebase
   - 驗證事件屬性使用正確的 data type
   - 驗證事件內容與舊版相同（如適用）

3. **`url_query` 欄位剪裁測試**：
   - 驗證超過 100 字元的 `url_query` 正確剪裁
   - 驗證剪裁後的內容正確上報

4. **`instance_id` 欄位測試**：
   - 驗證 Register Device API 包含 `instance_id` 欄位
   - 驗證 `instance_id` 值為 Firebase instance id

5. **邊緣案例測試**：
   - 網路異常時事件上報處理
   - 多個事件同時觸發時的處理
   - 事件屬性為 null 或空值的處理

---

## 📝 測試案例

## 1. 功能與業務邏輯測試 (Functional & Business Logic)

### 1.1 Firebase 事件名稱驗證測試 - Development Build
**測試目標**：驗證 Development Build 中事件名稱驗證機制正常運作

**前置條件**：
- Android 設備安裝 Development Build
- 準備 Firebase Console 或 Firebase DebugView 查看工具
- 準備觸發不符合命名規則的事件（如：包含大寫字母、特殊字元等）

**測試步驟**：
1. 啟動 Development Build App
2. 觸發不符合命名規則的事件（如：`InvalidEventName`、`event with space`、`event__with_double_underscore`）
3. 觀察是否顯示 Toast 訊息：
   - 預期顯示：`"Event($eventName) does not match the expected pattern"`
4. 檢查 Firebase Console：
   - 驗證不符合規則的事件不會上報到 Firebase
5. 觸發符合命名規則的事件（如：`valid_event_name`、`event_name_v2`）
6. 檢查 Firebase Console：
   - 驗證符合規則的事件正常上報

**預期結果**：
- Development Build 中驗證機制正常運作
- 不符合規則的事件顯示 Toast 訊息並取消上報
- 符合規則的事件正常上報
- 驗證規則符合規格：僅允許小寫字母、數字、底線，必須以小寫字母開頭，不能有連續底線

---

### 1.2 Firebase 事件屬性驗證測試 - Development Build
**測試目標**：驗證 Development Build 中事件屬性驗證機制正常運作

**前置條件**：
- Android 設備安裝 Development Build
- 準備 Firebase Console 查看工具
- 準備觸發包含不符合命名規則的屬性的事件

**測試步驟**：
1. 啟動 Development Build App
2. 觸發包含不符合命名規則的屬性的事件（如：`InvalidProperty`、`property with space`、`property__with_double_underscore`）
3. 觀察是否顯示 Toast 訊息：
   - 預期顯示：`"Event($eventName)\n\n Invalid Properties: \n - $propertyName1\n - $propertyName2\n - etc."`
4. 檢查 Firebase Console：
   - 驗證包含不符合規則屬性的事件不會上報到 Firebase
5. 觸發包含符合命名規則的屬性的事件
6. 檢查 Firebase Console：
   - 驗證符合規則的事件正常上報

**預期結果**：
- Development Build 中屬性驗證機制正常運作
- 不符合規則的屬性顯示 Toast 訊息並取消上報
- 符合規則的事件正常上報
- 驗證規則符合規格：僅允許小寫字母、數字、底線，必須以小寫字母開頭，不能有連續底線

---

### 1.3 Firebase 事件驗證測試 - Production Build
**測試目標**：驗證 Production Build 中不執行事件驗證，所有事件正常上報

**前置條件**：
- Android 設備安裝 Production Build
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 啟動 Production Build App
2. 觸發各種事件（包括不符合命名規則的事件，如果存在）
3. 觀察是否顯示 Toast 訊息：
   - 預期：不顯示任何驗證相關的 Toast 訊息
4. 檢查 Firebase Console：
   - 驗證所有事件正常上報到 Firebase
   - 驗證不會因為驗證機制而取消事件上報

**預期結果**：
- Production Build 中不執行驗證機制
- 所有事件正常上報到 Firebase
- 無驗證相關的 Toast 訊息
- 符合規格：驗證僅在 Development Build 中執行

---

### 1.4 login_experience_v2 事件上報測試
**測試目標**：驗證 `login_experience_v2` 事件正確上報，且內容與舊版 `login_experience` 相同

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 或 Firebase DebugView 查看工具
- 準備測試帳號（Email 登入、Google 登入、Apple 登入等）

**測試步驟**：
1. 使用測試帳號執行登入流程（Email 登入）
2. 檢查 Firebase Console：
   - 驗證 `login_experience_v2` 事件正確上報
   - 驗證事件內容與舊版 `login_experience` 相同（參考 [Login UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI)）
   - 詳細 data type 驗證請參考測試項目 1.15.1
3. 使用不同登入方式（Google 登入、Apple 登入等）重複測試
4. 檢查舊版 `login_experience` 事件：
   - 驗證舊版事件仍正常上報（不改動舊事件）

**預期結果**：
- `login_experience_v2` 事件正確上報到 Firebase
- 事件內容與舊版 `login_experience` 完全相同
- 舊版 `login_experience` 事件仍正常上報
- 不同登入方式都能正確觸發事件

---

### 1.5 live_experience_v2 事件上報測試
**測試目標**：驗證 `live_experience_v2` 事件正確上報，且內容與舊版 `live_experience` 相同

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（手機相機或硬體相機）
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台相機
3. 開啟即時影像（Live Feed）
4. 執行各種即時影像操作：
   - 觀看即時影像
   - 切換全螢幕
   - 開啟/關閉雙向語音
   - 截圖
   - 錄影
5. 關閉即時影像
6. 檢查 Firebase Console：
   - 驗證 `live_experience_v2` 事件正確上報
   - 驗證事件內容與舊版 `live_experience` 相同（參考 [Live UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI)）
   - 詳細 data type 驗證請參考測試項目 1.15.2
7. 檢查舊版 `live_experience` 事件：
   - 驗證舊版事件仍正常上報

**預期結果**：
- `live_experience_v2` 事件正確上報到 Firebase
- 事件內容與舊版 `live_experience` 完全相同
- 舊版 `live_experience` 事件仍正常上報
- 各種即時影像操作都能正確觸發事件

---

### 1.6 cr_playback_experience_v2 事件上報測試
**測試目標**：驗證 `cr_playback_experience_v2` 事件正確上報，且內容與舊版 `continuous_recording_playback_experience` 相同

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（支援連續錄影功能）
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台支援連續錄影的相機
3. 進入連續錄影播放頁面
4. 執行各種播放操作：
   - 播放錄影
   - 快轉/倒轉
   - 調整播放速度
   - 切換時間軸
   - 截圖
5. 關閉播放頁面
6. 檢查 Firebase Console：
   - 驗證 `cr_playback_experience_v2` 事件正確上報
   - 驗證事件內容與舊版 `continuous_recording_playback_experience` 相同（參考 [CR Playback UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI)）
   - 詳細 data type 驗證請參考測試項目 1.15.3
7. 檢查舊版 `continuous_recording_playback_experience` 事件：
   - 驗證舊版事件仍正常上報

**預期結果**：
- `cr_playback_experience_v2` 事件正確上報到 Firebase
- 事件內容與舊版 `continuous_recording_playback_experience` 完全相同
- 舊版 `continuous_recording_playback_experience` 事件仍正常上報
- 各種播放操作都能正確觸發事件

---

### 1.7 event_play_experience_v2 事件上報測試
**測試目標**：驗證 `event_play_experience_v2` 事件正確上報（之前沒有上報到 Firebase）

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（有事件錄影）
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台有事件錄影的相機
3. 進入事件列表，選擇一個事件
4. 播放事件錄影
5. 執行各種播放操作：
   - 播放錄影
   - 快轉/倒轉
   - 調整播放速度
   - 切換時間軸
   - 截圖
6. 關閉播放頁面
7. 檢查 Firebase Console：
   - 驗證 `event_play_experience_v2` 事件正確上報
   - 驗證事件內容符合規格（參考 [Event Play UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI)）
   - 詳細 data type 驗證請參考測試項目 1.15.4

**預期結果**：
- `event_play_experience_v2` 事件正確上報到 Firebase
- 事件內容符合規格
- 之前沒有上報到 Firebase，現在正確上報

---

### 1.8 payment_experience_v2 事件上報測試 - 正常 url_query
**測試目標**：驗證 `payment_experience_v2` 事件正確上報，且 `url_query` 欄位正確處理（正常長度）

**前置條件**：
- Android 設備已安裝 App
- 準備測試帳號（Free 用戶）
- 準備訂閱購買流程（測試環境或沙盒環境）
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用 Free 帳號登入 App
2. 進入訂閱購買頁面
3. 選擇訂閱方案（Premium 或 Plus）
4. 完成訂閱購買流程
5. 檢查 Firebase Console：
   - 驗證 `payment_experience_v2` 事件正確上報
   - 驗證 `url_query` 欄位存在且值正確（長度 ≤ 100 字元）
   - 驗證事件內容符合規格（參考 [Payment UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI)）
   - 詳細 data type 驗證請參考測試項目 1.15.5

**預期結果**：
- `payment_experience_v2` 事件正確上報到 Firebase
- `url_query` 欄位存在且值正確（長度 ≤ 100 字元）
- 事件內容符合規格
- 之前沒有上報到 Firebase，現在正確上報

---

### 1.9 payment_experience_v2 事件上報測試 - url_query 超過 100 字元剪裁
**測試目標**：驗證 `payment_experience_v2` 事件中 `url_query` 欄位超過 100 字元時正確剪裁

**前置條件**：
- Android 設備已安裝 App
- 準備測試帳號（Free 用戶）
- 準備訂閱購買流程（模擬或實際產生超過 100 字元的 url_query）
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用 Free 帳號登入 App
2. 進入訂閱購買頁面
3. 選擇訂閱方案
4. 完成訂閱購買流程（確保 url_query 超過 100 字元）
5. 檢查 Firebase Console：
   - 驗證 `payment_experience_v2` 事件正確上報
   - 驗證 `url_query` 欄位長度 ≤ 100 字元（已剪裁）
   - 驗證剪裁後的內容正確（如：保留前 100 字元）

**預期結果**：
- `payment_experience_v2` 事件正確上報到 Firebase
- `url_query` 欄位長度 ≤ 100 字元（已正確剪裁）
- 剪裁後的內容正確
- 超過 100 字元的部分被正確剪裁

---

### 1.10 camera_active_v2 事件上報測試
**測試目標**：驗證 `camera_active_v2` 事件正確上報

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發相機啟用相關操作（如：配對新相機、啟用相機等）
3. 檢查 Firebase Console：
   - 驗證 `camera_active_v2` 事件正確上報
   - 驗證事件內容符合規格（參考 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_active_v2)）
   - 詳細 data type 驗證請參考測試項目 1.15.6

**預期結果**：
- `camera_active_v2` 事件正確上報到 Firebase
- 事件內容符合規格

---

### 1.11 camera_statistics_v2 事件上報測試
**測試目標**：驗證 `camera_statistics_v2` 事件正確上報

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發相機統計相關操作（如：查看相機統計、統計資料更新等）
3. 檢查 Firebase Console：
   - 驗證 `camera_statistics_v2` 事件正確上報
   - 驗證事件內容符合規格（參考 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_statistics_v2)）
   - 詳細 data type 驗證請參考測試項目 1.15.7

**預期結果**：
- `camera_statistics_v2` 事件正確上報到 Firebase
- 事件內容符合規格

---

### 1.12 camera_settings_v2 事件上報測試
**測試目標**：驗證 `camera_settings_v2` 事件正確上報

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機設定頁面
3. 執行各種設定操作：
   - 修改相機名稱
   - 調整錄影品質
   - 開啟/關閉通知
   - 修改其他設定
4. 檢查 Firebase Console：
   - 驗證 `camera_settings_v2` 事件正確上報
   - 驗證事件內容符合規格（參考 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_settings_v2)）
   - 詳細 data type 驗證請參考測試項目 1.15.8

**預期結果**：
- `camera_settings_v2` 事件正確上報到 Firebase
- 事件內容符合規格
- 各種設定操作都能正確觸發事件

---

### 1.13 Register Device API - instance_id 欄位測試
**測試目標**：驗證 Register Device API 包含 `instance_id` 欄位，且值為 Firebase instance id

**前置條件**：
- Android 設備已安裝 App
- 準備 API 監控工具（如：Charles Proxy、Fiddler）
- 準備 Firebase Console 查看 Firebase instance id

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發裝置註冊流程（如：配對新相機、重新註冊裝置等）
3. 使用 API 監控工具攔截 Register Device API 請求
4. 檢查 API Request/Response：
   - 驗證 `instance_id` 欄位存在
   - 驗證 `instance_id` 值為 Firebase instance id (app_instance_id)
   - 驗證 `instance_id` 值格式正確
5. 從 Firebase Console 取得 Firebase instance id
6. 比對 API 中的 `instance_id` 與 Firebase instance id：
   - 驗證兩者一致

**預期結果**：
- Register Device API 包含 `instance_id` 欄位
- `instance_id` 值為 Firebase instance id (app_instance_id)
- `instance_id` 值格式正確
- API 中的 `instance_id` 與 Firebase instance id 一致
- 參考：[CAMERA-5617](https://alfredlabs.atlassian.net/browse/CAMERA-5617)

---

### 1.14 舊版事件不變動驗證測試
**測試目標**：驗證所有舊版 Firebase Analytics 事件仍正常上報，未被改動

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 查看工具
- 準備觸發各種舊版事件的場景

**測試步驟**：
1. 使用測試帳號登入 App
2. 執行各種操作，觸發舊版事件：
   - 登入：觸發 `login_experience`
   - 即時影像：觸發 `live_experience`
   - 連續錄影播放：觸發 `continuous_recording_playback_experience`
   - 其他舊版事件
3. 檢查 Firebase Console：
   - 驗證所有舊版事件仍正常上報
   - 驗證舊版事件內容未被改動
   - 驗證舊版事件與新版事件（如適用）同時上報

**預期結果**：
- 所有舊版事件仍正常上報
- 舊版事件內容未被改動
- 舊版事件與新版事件（如適用）同時上報
- 符合規格：不改動任何舊的事件

---

### 1.15 _v2 新事件 Data Type 驗證測試

#### 1.15.1 login_experience_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `login_experience_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 或 Firebase DebugView 查看工具
- 準備測試帳號（Email 登入、Google 登入、Apple 登入等）
- 準備 [Login UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI) 作為參考

**測試步驟**：
1. 使用測試帳號執行登入流程
2. 在 Firebase Console 中查看 `login_experience_v2` 事件
3. 逐一檢查每個事件屬性的 data type：
   - 根據 [Login UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式
4. 使用不同登入方式（Email、Google、Apple 等）重複測試
5. 比對舊版 `login_experience` 事件的 data type：
   - 驗證 `login_experience_v2` 與舊版 `login_experience` 的 data type 完全相同

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式
- `login_experience_v2` 與舊版 `login_experience` 的 data type 完全相同

---

#### 1.15.2 live_experience_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `live_experience_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（手機相機或硬體相機）
- 準備 Firebase Console 查看工具
- 準備 [Live UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台相機
3. 開啟即時影像（Live Feed）
4. 執行各種即時影像操作後關閉
5. 在 Firebase Console 中查看 `live_experience_v2` 事件
6. 逐一檢查每個事件屬性的 data type：
   - 根據 [Live UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式
7. 比對舊版 `live_experience` 事件的 data type：
   - 驗證 `live_experience_v2` 與舊版 `live_experience` 的 data type 完全相同

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式
- `live_experience_v2` 與舊版 `live_experience` 的 data type 完全相同

---

#### 1.15.3 cr_playback_experience_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `cr_playback_experience_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（支援連續錄影功能）
- 準備 Firebase Console 查看工具
- 準備 [CR Playback UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台支援連續錄影的相機
3. 進入連續錄影播放頁面
4. 執行各種播放操作後關閉
5. 在 Firebase Console 中查看 `cr_playback_experience_v2` 事件
6. 逐一檢查每個事件屬性的 data type：
   - 根據 [CR Playback UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式
7. 比對舊版 `continuous_recording_playback_experience` 事件的 data type：
   - 驗證 `cr_playback_experience_v2` 與舊版 `continuous_recording_playback_experience` 的 data type 完全相同

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式
- `cr_playback_experience_v2` 與舊版 `continuous_recording_playback_experience` 的 data type 完全相同

---

#### 1.15.4 event_play_experience_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `event_play_experience_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機（有事件錄影）
- 準備 Firebase Console 查看工具
- 準備 [Event Play UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機列表，選擇一台有事件錄影的相機
3. 進入事件列表，選擇一個事件
4. 播放事件錄影並執行各種播放操作後關閉
5. 在 Firebase Console 中查看 `event_play_experience_v2` 事件
6. 逐一檢查每個事件屬性的 data type：
   - 根據 [Event Play UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式

---

#### 1.15.5 payment_experience_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `payment_experience_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備測試帳號（Free 用戶）
- 準備訂閱購買流程（測試環境或沙盒環境）
- 準備 Firebase Console 查看工具
- 準備 [Payment UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI) 作為參考

**測試步驟**：
1. 使用 Free 帳號登入 App
2. 進入訂閱購買頁面
3. 選擇訂閱方案（Premium 或 Plus）
4. 完成訂閱購買流程
5. 在 Firebase Console 中查看 `payment_experience_v2` 事件
6. 逐一檢查每個事件屬性的 data type：
   - 根據 [Payment UXI Spec](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式（包括 `url_query` 欄位）
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式
7. 特別驗證 `url_query` 欄位：
   - 驗證 `url_query` 為 String 類型
   - 驗證長度 ≤ 100 字元（如超過 100 字元需剪裁）

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式
- `url_query` 欄位為 String 類型，長度 ≤ 100 字元

---

#### 1.15.6 camera_active_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `camera_active_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具
- 準備 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_active_v2) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發相機啟用相關操作（如：配對新相機、啟用相機等）
3. 在 Firebase Console 中查看 `camera_active_v2` 事件
4. 逐一檢查每個事件屬性的 data type：
   - 根據 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_active_v2) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式

---

#### 1.15.7 camera_statistics_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `camera_statistics_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具
- 準備 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_statistics_v2) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發相機統計相關操作（如：查看相機統計、統計資料更新等）
3. 在 Firebase Console 中查看 `camera_statistics_v2` 事件
4. 逐一檢查每個事件屬性的 data type：
   - 根據 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_statistics_v2) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式

---

#### 1.15.8 camera_settings_v2 事件 Data Type 驗證測試
**測試目標**：驗證 `camera_settings_v2` 事件所有屬性的 data type 正確

**前置條件**：
- Android 設備已安裝 App
- 準備已配對的相機
- 準備 Firebase Console 查看工具
- 準備 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_settings_v2) 作為參考

**測試步驟**：
1. 使用測試帳號登入 App
2. 進入相機設定頁面
3. 執行各種設定操作（修改相機名稱、調整錄影品質、開啟/關閉通知等）
4. 在 Firebase Console 中查看 `camera_settings_v2` 事件
5. 逐一檢查每個事件屬性的 data type：
   - 根據 [Camera Active v2 Spec](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_settings_v2) 驗證每個屬性的 data type
   - String 類型屬性：驗證值為字串格式
   - Number 類型屬性：驗證值為數字格式
   - Boolean 類型屬性：驗證值為 true/false
   - Array 類型屬性：驗證值為陣列格式

**預期結果**：
- 所有事件屬性的 data type 符合規格
- String 類型屬性正確為字串格式
- Number 類型屬性正確為數字格式
- Boolean 類型屬性正確為 true/false
- Array 類型屬性正確為陣列格式

---

## 2. 穩定性與可靠性測試 (Stability & Reliability)

### 2.1 網路異常時事件上報測試
**測試目標**：驗證網路異常時事件上報的處理機制

**前置條件**：
- Android 設備已安裝 App
- 準備弱網環境或網路中斷模擬工具
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發各種事件（登入、即時影像、訂閱購買等）
3. 在事件上報過程中模擬網路異常：
   - 網路中斷
   - 弱網環境（3G 速度）
   - API 錯誤（500、503）
4. 觀察 App 行為：
   - 驗證 App 不會崩潰
   - 驗證事件上報有適當的錯誤處理
5. 恢復網路連線
6. 檢查 Firebase Console：
   - 驗證事件是否在網路恢復後重新上報
   - 驗證事件資料完整性

**預期結果**：
- 網路異常時 App 不會崩潰
- 事件上報有適當的錯誤處理
- 網路恢復後事件能重新上報
- 事件資料完整性不受影響

---

### 2.2 多個事件同時觸發測試
**測試目標**：驗證多個事件同時觸發時的上報處理

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 快速執行多個操作，同時觸發多個事件：
   - 登入後立即進入即時影像
   - 同時開啟多個相機的即時影像
   - 同時執行訂閱購買和相機設定
3. 檢查 Firebase Console：
   - 驗證所有事件都正確上報
   - 驗證事件不會遺失
   - 驗證事件屬性正確

**預期結果**：
- 所有事件都正確上報
- 事件不會遺失
- 事件屬性正確
- 無競態條件或資料錯誤

---

### 2.3 事件屬性為 null 或空值處理測試
**測試目標**：驗證事件屬性為 null 或空值時的處理機制

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 查看工具
- 準備觸發屬性為 null 或空值的場景

**測試步驟**：
1. 使用測試帳號登入 App
2. 觸發各種事件，包含 null 或空值屬性：
   - 登入時某些屬性為 null
   - 訂閱購買時 `url_query` 為空字串
   - 相機設定時某些屬性為 null
3. 檢查 Firebase Console：
   - 驗證事件仍正常上報
   - 驗證 null 或空值屬性正確處理（如：不包含在事件中或使用預設值）

**預期結果**：
- 事件仍正常上報
- null 或空值屬性正確處理
- 無錯誤或異常

---

### 2.4 長時間使用後事件上報穩定性測試
**測試目標**：驗證長時間使用 App 後事件上報機制仍正常運作

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 正常使用 App 30 分鐘以上
3. 執行多次操作，觸發各種事件：
   - 登入/登出
   - 即時影像
   - 連續錄影播放
   - 事件播放
   - 訂閱購買
   - 相機設定
4. 每次操作後檢查 Firebase Console：
   - 驗證事件正確上報
5. 檢查 App 記憶體使用和效能

**預期結果**：
- 長時間使用後事件上報機制仍正常運作
- 無記憶體洩漏
- 無效能下降
- 事件上報準確性不變

---

## 3. 使用者體驗測試 (User Experience)

### 3.1 事件上報對用戶體驗影響測試
**測試目標**：驗證事件上報機制不影響用戶體驗

**前置條件**：
- Android 設備已安裝 App
- 準備 Firebase Console 查看工具

**測試步驟**：
1. 使用測試帳號登入 App
2. 執行各種操作（登入、即時影像、訂閱購買等）
3. 觀察用戶體驗：
   - 操作是否順暢
   - 是否有明顯延遲
   - 是否有載入指示器（不應有）
4. 檢查 Firebase Console：
   - 驗證事件正確上報

**預期結果**：
- 操作順暢，無明顯延遲
- 事件上報不影響用戶體驗
- 無不必要的載入指示器
- 事件正確上報

---

### 3.2 Development Build 驗證 Toast 訊息體驗測試
**測試目標**：驗證 Development Build 中驗證失敗時的 Toast 訊息不影響用戶體驗

**前置條件**：
- Android 設備安裝 Development Build
- 準備觸發不符合命名規則的事件

**測試步驟**：
1. 啟動 Development Build App
2. 觸發不符合命名規則的事件
3. 觀察 Toast 訊息：
   - 驗證 Toast 訊息顯示時間適當（不會過長）
   - 驗證 Toast 訊息內容清晰
   - 驗證 Toast 訊息不阻擋用戶操作
4. 繼續使用 App，驗證功能正常

**預期結果**：
- Toast 訊息顯示時間適當
- Toast 訊息內容清晰
- Toast 訊息不阻擋用戶操作
- App 功能正常運作

---

## 📊 測試總結

### 測試覆蓋範圍
- ✅ 功能與業務邏輯測試（22 個測試案例）
  - 事件上報測試（14 個）
  - Data Type 驗證測試（8 個）
- ✅ 穩定性與可靠性測試（4 個測試案例）
- ✅ 使用者體驗測試（2 個測試案例）

**總計：28 個測試案例**

### 關鍵測試項目
1. **Firebase 事件驗證機制**：Development Build 中驗證失敗時顯示 Toast 訊息並取消上報，Production Build 中不執行驗證
2. **新事件上報測試**：8 個新事件（`login_experience_v2`、`live_experience_v2`、`cr_playback_experience_v2`、`event_play_experience_v2`、`payment_experience_v2`、`camera_active_v2`、`camera_statistics_v2`、`camera_settings_v2`）正確上報
3. **事件內容驗證**：新事件內容與舊版相同（如適用）
4. **_v2 新事件 Data Type 驗證**：8 個新事件的所有屬性 data type 正確驗證（獨立測試項目 1.15.1-1.15.8）
5. **`url_query` 欄位剪裁測試**：超過 100 字元時正確剪裁
6. **`instance_id` 欄位測試**：Register Device API 包含 `instance_id` 欄位，值為 Firebase instance id
7. **舊版事件不變動驗證**：所有舊版事件仍正常上報，未被改動
8. **網路異常測試**：網路異常時事件上報的處理機制

### 風險評估
- **高風險項目**：
  - Firebase 事件驗證機制（可能影響事件上報）
  - 新事件上報機制（可能影響資料收集）
  - `url_query` 欄位剪裁（可能導致資料遺失）
- **中風險項目**：
  - Register Device API 變更（可能影響裝置註冊）
  - 向下兼容性（舊版 Client 可能受影響）

### 測試建議
1. **優先測試項目**：
   - Firebase 事件驗證機制（Development Build 和 Production Build）
   - 新事件上報測試（8 個新事件）
   - 舊版事件不變動驗證

2. **重點驗證項目**：
   - 事件內容與舊版相同（如適用）
   - 事件屬性 data type 驗證（獨立測試項目 1.15.1-1.15.8）
   - `url_query` 欄位剪裁（超過 100 字元）
   - `instance_id` 欄位正確性

3. **監控指標**：
   - 事件上報成功率 > 95%
   - 事件驗證失敗率 < 5%（Development Build）
   - 用戶體驗不受影響

---

## 📝 測試記錄

### 測試環境
- **測試日期**：待填寫
- **測試人員**：待填寫
- **測試設備**：
  - Android 設備：待填寫
  - **Android 版本**：待填寫（需測試最新官方 Android 版本，基於 Test_Scenario.md）
- **App 版本**：Android - 2026.1.0
- **Build 類型**：
  - Development Build：待填寫
  - Production Build：待填寫
- **測試場景**：待填寫
- **測試帳號**：
  - Free 帳號：待填寫
  - Premium 帳號：待填寫
  - Plus 帳號：待填寫
  - 硬體相機 Owner 帳號：待填寫

### 測試結果
- **通過項目**：待填寫
- **失敗項目**：待填寫
- **阻塞問題**：待填寫
- **已知問題**：待填寫

---

## 🔗 相關連結
- **Jira Ticket**: [CAMERA-6225](https://alfredlabs.atlassian.net/browse/CAMERA-6225)
- **Parent Epic**: [CAMERA-6050](https://alfredlabs.atlassian.net/browse/CAMERA-6050) - Firebase Analytics Reboot – Stage 1 (Core User Properties + UXI + Camera Related Events)
- **技術規格文件**:
  - [Event Name and Property Validation](https://alfredlabs.atlassian.net/wiki/spaces/PD/pages/3900801027/Event+Name+and+Property+Validation)
  - [Login UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI)
  - [Live UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI)
  - [CR Playback UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI)
  - [Event Play UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI)
  - [Payment UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI)
  - [Camera Active v2](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2)
- **GitHub PR**: [PR #4073](https://github.com/alfred-systems/iVuu/pull/4073)
- **產品專有名詞**: [Terminology Table v2](https://alfredlabs.atlassian.net/wiki/spaces/~590960658/pages/3834085744/Terminology+Table+v2) - 阿褔管家 APP 產品專有名詞參考
- **參考 Ticket**: [CAMERA-5617](https://alfredlabs.atlassian.net/browse/CAMERA-5617) - Register Device API instance_id 欄位參考

---

## 📚 技術細節參考

### Firebase 事件驗證規則
1. **Event Name Validation**：
   - 僅允許小寫字母（a-z）、數字（0-9）、底線（_）
   - 必須以小寫字母開頭
   - 必須以小寫字母或數字結尾
   - 不能有連續底線
   - 符合 regex: `[a-z]([0-9a-z]*_[0-9a-z]+|[0-9a-z]+)*`

2. **Event Property Validation**：
   - 與 Event Name 相同的驗證規則
   - 符合 regex: `[a-z]([0-9a-z]*_[0-9a-z]+|[0-9a-z]+)*`

3. **驗證執行環境**：
   - Development Build：執行驗證，驗證失敗時顯示 Toast 訊息並取消上報
   - Production Build：不執行驗證，所有事件正常上報

### 新增 Firebase Analytics 事件列表
| Event Name | 舊版事件 | 說明 | Spec 連結 |
| --- | --- | --- | --- |
| `login_experience_v2` | `login_experience` | 與舊版內容相同 | [Login UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866984525/Login+UXI) |
| `live_experience_v2` | `live_experience` | 與舊版內容相同 | [Live UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866919045/Live+UXI) |
| `cr_playback_experience_v2` | `continuous_recording_playback_experience` | 與舊版內容相同 | [CR Playback UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574345/CR+Playback+UXI) |
| `event_play_experience_v2` | 無（新增） | 之前沒有上報到 Firebase | [Event Play UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3867574358/Event+Play+UXI) |
| `payment_experience_v2` | 無（新增） | 之前沒有上報到 Firebase，需包含 `url_query` 欄位（超過 100 字元需剪裁） | [Payment UXI](https://alfredlabs.atlassian.net/wiki/spaces/SPEC/pages/3866951835/Payment+UXI) |
| `camera_active_v2` | 無（新增） | Camera 相關事件 | [Camera Active v2](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_active_v2) |
| `camera_statistics_v2` | 無（新增） | Camera 相關事件 | [Camera Active v2](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_statistics_v2) |
| `camera_settings_v2` | 無（新增） | Camera 相關事件 | [Camera Active v2](https://alfredlabs.atlassian.net/wiki/spaces/~805741080/pages/4528799805/Camera+Active+v2#camera_settings_v2) |

### Register Device API 變更
- **新增欄位**：`instance_id`
- **欄位類型**：String
- **欄位內容**：Firebase instance id (app_instance_id)
- **參考**：[CAMERA-5617](https://alfredlabs.atlassian.net/browse/CAMERA-5617)

### 重要原則
- ⚠️ **不改動任何舊的事件！**
- 新事件必須使用正確的 data type
- 事件驗證僅在 Development Build 中執行
- Production Build 中不執行驗證，所有事件正常上報

